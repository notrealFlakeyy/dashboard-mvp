# Dependency installation permission

Saved: 2026-05-26

User explicitly allowed installing necessary dependencies for the Next.js + Supabase business dashboard MVP build.

Allowed:

- npm / npx dependencies needed for Next.js app scaffolding/build
- Supabase CLI/client packages if needed
- TypeScript/lint/build tooling
- Other free/open-source packages required to complete the local MVP

Constraints still apply:

- Do not spend money or create paid resources.
- Do not expose secrets.
- Use the user's Supabase account only if credentials/config are already available or explicitly provided.
- If Supabase credentials are missing, prepare local migrations/setup docs instead.
